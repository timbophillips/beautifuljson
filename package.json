{
  "name": "colorjson",
  "version": "1.0.0",
  "description": "Typescript library using chalk and json-colorizer together to print easy to read JSON for the command line",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test-ts": "ts-node lib/test.ts",
    "test-js": "tsc && node dist/test.js"
  },
  "bin": {
    "bj": "dist/bj.js"
  },
  "author": "Tim Phillips",
  "license": "ISC",
  "dependencies": {
    "chalk": "^2.4.1"
  },
  "devDependencies": {
    "@types/node": "^10.12.7",
    "typescript": "^4.6.2"
  }
}
