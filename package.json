{
  "name": "beautifuljson",
  "version": "1.0.0",
  "description": "Typescript library using chalk and json-colorizer together to print easy to read JSON for the command line",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tim Phillips",
  "license": "ISC",
  "dependencies": {
    "chalk": "^2.4.1"
  },
  "devDependencies": {
    "@types/node": "^10.12.7"
  }
}
