{
  "name": "@timbophillips/colorjson",
  "version": "1.1.2",
  "description": "Typescript library using chalk and json-colorizer together to print easy to read JSON for the command line",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test-ts": "ts-node lib/test.ts",
    "test": "tsc && node dist/test.js",
    "prepublish": "tsc"
  },
  "bin": {
    "colourjson": "dist/colorjson.js"
  },
  "author": "Tim Phillips",
  "license": "ISC",
  "dependencies": {
    "chalk": "^2.4.1"
  },
  "devDependencies": {
    "@types/node": "^10.17.60",
    "typescript": "^4.6.2"
  }
}
